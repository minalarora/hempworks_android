package com.hemp.works.dashboard.tnl.ui.adapters

import com.hemp.works.dashboard.model.LiveSession

class LiveSessionViewModel(private val liveSession: LiveSession) {

    val title = liveSession.title
}